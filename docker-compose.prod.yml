# Production qatlami:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
# Tashqariga faqat Caddy (80/443) ochiq; qolgan servislar ichki tarmoqda.
services:
  db:
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD .env da berilishi kerak}

  redis:
    restart: unless-stopped

  ai-service:
    restart: unless-stopped

  backend:
    restart: unless-stopped
    environment:
      ENVIRONMENT: production
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY kerak}
      SEED_DEMO_DATA: "false"
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-agro}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-agroyordam}
      CORS_ORIGINS: https://${DOMAIN}
      PAYMENT_RETURN_URL: https://${DOMAIN}/premium

  web:
    restart: unless-stopped
    ports: !reset []

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on: [web]
    ports: ["80:80", "443:443", "443:443/udp"]
    environment:
      DOMAIN: ${DOMAIN:?DOMAIN kerak (masalan agroyordam.uz)}
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

  # Har kuni PostgreSQL va rasmlar zaxira nusxasi → ./backups (7 kun saqlanadi)
  backup:
    image: postgres:16-alpine
    restart: unless-stopped
    depends_on:
      db: {condition: service_healthy}
    environment:
      PGPASSWORD: ${POSTGRES_PASSWORD}
    volumes: ["./backups:/backups", "media:/media:ro"]
    entrypoint: ["sh", "-c", "while true; do d=$$(date +%F); pg_dump -h db -U ${POSTGRES_USER:-agro} ${POSTGRES_DB:-agroyordam} | gzip > /backups/agroyordam-$$d.sql.gz && tar czf /backups/media-$$d.tar.gz -C /media . && find /backups -name '*.gz' -mtime +7 -delete; sleep 86400; done"]

volumes:
  caddy_data:
  caddy_config:
